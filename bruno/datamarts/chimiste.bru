meta {
  name: Chimiste
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/datamarts/chimiste?limit=20&offset=0
  body: none
  auth: bearer
}

params:query {
  limit: 20
  offset: 0
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Narrow-band targets (HST filters mapped to emission lines).
  No time dimension --- pagination only.
}
